# Orange ShortUrl - 环境变量配置示例
# 使用方法：复制此文件为 .env 并修改为实际的配置值
# 命令：cp env.example .env

# ==================== 数据库配置 ====================
# MySQL root 密码
MYSQL_ROOT_PASSWORD=change_me_to_strong_password

# 数据库名称
MYSQL_DATABASE=shorturl

# ==================== Redis 配置 ====================
# Redis 密码（建议设置）
REDIS_PASSWORD=change_me_to_strong_password

# ==================== 邮件服务配置 ====================
# SMTP 服务器地址（示例：smtp.qq.com, smtp.gmail.com, smtp.163.com）
MAIL_HOST=smtp.qq.com

# SMTP 端口（一般 465 用于 SSL，587 用于 TLS）
MAIL_PORT=465

# 邮箱账号
MAIL_USERNAME=your-email@qq.com

# 邮箱密码或授权码（QQ邮箱、网易邮箱等需要使用授权码，而不是登录密码）
MAIL_PASSWORD=your-email-auth-code

# ==================== 应用配置 ====================
# 短链接域名（生产环境请修改为实际域名）
DOMAIN=https://your-domain.com

# ==================== OAuth 配置（可选）====================
# 如果不使用 Linux.do OAuth 登录，可以删除或留空这些配置

# Linux.do OAuth 客户端ID
# 申请地址：https://connect.linux.do/
OAUTH_CLIENT_ID=your-linuxdo-client-id

# Linux.do OAuth 客户端密钥
OAUTH_CLIENT_SECRET=your-linuxdo-client-secret

# ==================== 其他说明 ====================
# 1. 请勿将 .env 文件提交到版本控制系统（已在.gitignore中）
# 2. 密码请使用强密码，包含大小写字母、数字和特殊字符
# 3. QQ邮箱授权码获取方式：
#    - 登录QQ邮箱 → 设置 → 账户 → POP3/IMAP/SMTP/Exchange/CardDAV/CalDAV服务
#    - 开启"POP3/SMTP服务"或"IMAP/SMTP服务"
#    - 生成授权码并复制到 MAIL_PASSWORD
# 4. 生产环境部署时，请务必修改所有 "change_me" 和 "your-" 开头的配置
# 5. Docker Compose 会自动读取 .env 文件中的变量

